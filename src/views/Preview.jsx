const React = require('react');

const Layout = require('./Layout');

module.exports = function Preview() {
  return (
    <Layout>
      <h1>Это главная страница для всех!</h1>
    </Layout>
  );
};
